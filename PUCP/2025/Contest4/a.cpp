#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

signed main() {
    freopen("input.txt", "r", stdin);
    ll a1, a2;
    while(cin >> a1) {
        cin >> a2;
        cout << abs(a1 - a2) << endl; 
        
    }
    return 0;
}